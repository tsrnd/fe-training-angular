import { HoverItemDirective } from './hover-item.directive';

describe('HoverItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverItemDirective();
    expect(directive).toBeTruthy();
  });
});
