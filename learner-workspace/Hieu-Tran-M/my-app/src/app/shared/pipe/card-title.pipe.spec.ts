import { CardTitlePipe } from './card-title.pipe';

describe('CardTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CardTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
