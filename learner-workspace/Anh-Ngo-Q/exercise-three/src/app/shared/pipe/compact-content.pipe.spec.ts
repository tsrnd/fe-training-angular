import { CompactContentPipe } from './compact-content.pipe';

describe('CompactContentPipe', () => {
  it('create an instance', () => {
    const pipe = new CompactContentPipe();
    expect(pipe).toBeTruthy();
  });
});
