import { CollapseTextPipe } from './collapse-text.pipe';

describe('CollapseTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CollapseTextPipe();
    expect(pipe).toBeTruthy();
  });
});
